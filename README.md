# A React Based App


## Available Scripts

In the project directory, you can run:

### `json-server --watch ./data/db.json`

Starts a JSON server, watches for changes in the db.json file, and serves the data from that file through a RESTful API. 

### `npm start`

Runs the app in the development mode.



